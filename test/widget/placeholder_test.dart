import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test — app builds without crash', (tester) async {
    // Will be replaced with real tests in Phase 1 TDD
    expect(1 + 1, equals(2));
  });
}
